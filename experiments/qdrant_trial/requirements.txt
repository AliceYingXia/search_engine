# ── BGE-M3 (local) ────────────────────────────────────────────────────────────
# FlagEmbedding 1.3.5 + transformers 5.x has a broken import in the reranker
# module (is_torch_fx_available removed). embed_chunks.py patches this at
# runtime — do NOT upgrade FlagEmbedding without re-testing the import.
FlagEmbedding==1.3.5
transformers==5.3.0
torch==2.10.0

# ── Voyage AI (API) ───────────────────────────────────────────────────────────
voyageai==0.3.7

# ── OpenAI (API) ──────────────────────────────────────────────────────────────
openai==2.29.0
httpx==0.28.1           # OpenAI SDK uses httpx internally
certifi==2026.2.25      # CA bundle for SSL verification
pip-system-certs==5.3   # macOS: makes httpx trust the system Keychain

# ── Qdrant ────────────────────────────────────────────────────────────────────
qdrant-client==1.17.1

# ── Shared utilities ──────────────────────────────────────────────────────────
python-dotenv==1.2.2    # load .env variables
