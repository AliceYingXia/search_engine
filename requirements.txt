# Evaluation Dataset Generation with Sasha Data
# Pipeline: sampling → cleaning → step text → consistency check → eval dataset synthesis
requests
# Data processing
pandas==3.0.1
pyarrow
fastparquet
openpyxl
sentence-transformers

matplotlib

psycopg2-binary01_sampling 
opensearch-py

# LLM API (GPT-4o for query generation, relevance assessment, consistency checks)
openai==2.29.0
httpx==0.28.1

# Stemming for dictionary lookup in search agent (Porter stemmer)
nltk

# Environment variable loading (.env for API keys)
python-dotenv==1.2.2
langfuse

# SSL / certificate handling (macOS)
certifi==2026.2.25
pip-system-certs==5.3
